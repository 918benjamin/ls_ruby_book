=begin
The first program outputs x as 3, since x was initialized outside
the block used by the times method.

The second program throws an error since x is defined within the
block and then is called outside the block.
=end